<Application x:Class="KabalistusIsaacTools.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KabalistusIsaacTools"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="White"/>
        <Style x:Key="CustomPanelBackground" TargetType="{x:Type Panel}">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
        </Style>

        <SolidColorBrush x:Key="ForegroundColor" Color="Black"/>
        <Style x:Key="CustomControlForeground" TargetType="{x:Type Control}">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
        </Style>
    </Application.Resources>
</Application>
